<?php

	$connection = new PDO("oci:dbname=venus", "jgautier", "nakross"); 
	

	$test = $connection->query('SELECT Nom, Prenom FROM Joueur');  
	$test->setFetchMode(PDO::FETCH_ASSOC);
  
	while($row = $test->fetch()) {  
        	echo $row['Nom'] . " " . $row['Prenom'] . "<br/>";  
	} 
?>
